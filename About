import React from "react";
import styled from "styled-components";
import { motion } from "framer-motion";

const About = () => {
  return (
    <AboutSection id="about">
      <motion.div
        initial={{ opacity: 0, x: -50 }}
        animate={{ opacity: 1, x: 0 }}
        transition={{ duration: 0.8 }}
      >
        <h2>About Me</h2>
        <p>
          I am a motivated Data Analyst with expertise in Python, SQL, and cloud technologies like AWS and Azure. 
          With a strong background in data visualization, machine learning, and database management, I help businesses 
          drive strategic decisions using data-driven insights.
        </p>
      </motion.div>
    </AboutSection>
  );
};

export default About;

const AboutSection = styled.section`
  padding: 80px 20px;
  text-align: center;
  background: #252542;
  color: white;

  h2 {
    font-size: 2.5rem;
    color: #f39c12;
  }

  p {
    font-size: 1.2rem;
    max-width: 800px;
    margin: auto;
  }
`;
